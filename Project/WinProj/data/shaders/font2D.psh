#version 330

in vec2 TexCoord;

out vec4 FragColor;

uniform sampler2D 	texture0;
//uniform vec4 		color;

void main()
{
	FragColor = vec4(1.0,1.0,1.0,1.0);
	//FragColor = texture(texture0, TexCoord);
	//FragColor = (vTexColor.r, vTexColor.r, vTexColor.r, vTexColor.r)*color;
}